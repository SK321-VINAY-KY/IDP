# Engineer A — Layer 1 (routing) + Layer 2 (conversion) dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Page inspection (Layer 1 Step A)
pymupdf>=1.28.0

# Digital conversion
docling==2.4.0

# OCR — both scanned (printed) and handwritten routes (CPU)
# PaddleOCR's built-in DBNet detector handles line segmentation automatically,
# replacing TrOCR's manual line-splitting + whole-page collapse approach.
paddleocr==2.7.3
#paddlepaddle==2.6.1        # CPU build (commented out for automated install on this Python version)
# Note: paddlepaddle CPU builds often need platform-specific wheels; uncomment
# and install manually if you need the handwritten OCR route on Windows.

# Image handling (still needed for PDF page rendering in tests)
pillow>=11.3.0

# Ollama VLM classification + transcription client (top tier of escalation ladder)
instructor==1.4.3
openai==1.44.1

# Schemas, config, resilience
pydantic==1.10.13
tenacity==8.5.0

# Testing
pytest==8.3.2

# --- Removed (2026-08-20) ---
# transformers and torch have been removed — TrOCR has been replaced by
# PaddleOCR for the handwritten route. This significantly reduces the CPU
# footprint: torch alone was ~2 GB; paddlepaddle CPU is ~200 MB.
# If TrOCR needs to be restored, re-add:
#   transformers==4.44.2
#   torch==2.4.1  (CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu)
