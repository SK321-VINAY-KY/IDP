# Engineer A — Layer 1 (routing) + Layer 2 (conversion) dependencies
# Install: pip install -r requirements.txt

# Page inspection (Layer 1 Step A)
pymupdf>=1.28.0

# Digital conversion
docling>=2.4.0

# OCR — both scanned (printed) and handwritten routes (CPU)
# PaddleOCR's built-in DBNet detector handles line segmentation automatically,
# replacing TrOCR's manual line-splitting + whole-page collapse approach.
paddleocr>=2.7.3
paddlepaddle>=2.6.0; sys_platform == 'linux' or sys_platform == 'darwin'

# Image & array handling
pillow>=11.0.0
numpy>=1.24.0

# LLM / VLM clients (escalation ladder & Step B ambiguous routing)
instructor>=1.4.3
openai>=1.44.1
httpx>=0.27.0
google-generativeai>=0.8.0

# Schemas, config, resilience
pydantic>=2.0.0
pydantic-settings>=2.0.0
tenacity>=8.5.0

# Web server & API Console (Serving Frontend & Pipeline API)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Testing
pytest>=8.0.0

# --- Removed (2026-08-20) ---
# transformers and torch have been removed — TrOCR has been replaced by
# PaddleOCR for the handwritten route. This significantly reduces the CPU
# footprint: torch alone was ~2 GB; paddlepaddle CPU is ~200 MB.
