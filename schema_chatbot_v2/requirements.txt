fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.0
httpx>=0.27
python-dotenv>=1.0
python-multipart>=0.0.9   # needed by FastAPI for the /schema/infer file-upload route
boto3>=1.34               # only needed at runtime if LLM_PROVIDER=bedrock
sarvamai>=0.1             # only needed at runtime for LLM_PROVIDER=sarvam document intake (Document AI / digitise)
pytest>=8.2

# Document & image dependencies for pipeline triggers
pymupdf>=1.28.0
pillow>=11.0.0
numpy>=1.24.0
sqlalchemy>=2.0
reportlab>=4.0
jinja2>=3.1

# Authentication & authorization
python-jose[cryptography]>=3.3
bcrypt>=4.0.0
